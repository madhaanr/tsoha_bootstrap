{% extends "base.html" %}
{% block content %}
    <h1>Askareet</h1>
    <p>
        <a href="askareenlisays" class="btn btn-default">Lisää askare</a>  
    </p>
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Nimi</th>
                <th>Lisäyspäivä</th>
                <th>Tärkeysaste</th>
                <th>Luokka</th>
                <th>Kuvaus</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            {% for askare in askareet %}
            <tr>
                <td><a href="{{base_path}}/askare/{{askare.id}}">{{askare.nimi}}</td>
                <td>{{askare.lisatty}}</td>
                <td>{{askare.tarkeys}}</td>
                <td>{{askare.luokka}}</td>
                <td>{{askare.kuvaus}}</td>
                <td><a href="askareenmuokkaus" class="btn btn-default">Muokkaa</a></td>
                <td><button class="btn btn-danger">Poista</button></td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}